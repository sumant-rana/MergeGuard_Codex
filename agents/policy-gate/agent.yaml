name: policy-gate
framework: langgraph
entrypoint: policy_gate.main:app
description: Policy guardrails agent that evaluates team YAML review rules against concept findings and emits pass/warn/block decisions.
features:
  memory: true
  guardrails: true
config:
  provider: local
  model: deterministic-demo
