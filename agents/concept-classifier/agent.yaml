name: concept-classifier
framework: langgraph
entrypoint: concept_classifier.main:app
description: Concept classifier agent for auth, PII, billing, SQL, prompt, agent, timeout, retry, and side-effect concepts.
features:
  memory: true
  guardrails: true
config:
  provider: anthropic
  model: claude-opus-4-7
