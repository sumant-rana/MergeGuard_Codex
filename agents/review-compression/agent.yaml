name: review-compression
framework: langgraph
entrypoint: review_compression.main:app
description: Deterministic change triage agent for changed-file classification, owner routing, hotspots, and safe-to-skim partitioning.
features:
  memory: false
  guardrails: false
config:
  provider: local
  model: deterministic
