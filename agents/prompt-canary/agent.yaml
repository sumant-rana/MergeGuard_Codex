name: prompt-canary
framework: langgraph
entrypoint: prompt_canary.main:app
description: Prompt drift check agent for prompt/model/agent workflow drift, JSON format, refusal, latency, and cost checks.
features:
  memory: true
  guardrails: true
config:
  provider: local
  model: deterministic-demo
