[project]
name = "mergeguard-agent-contract-comparator"
version = "0.2.0"
description = "MergeGuard contract-comparator Magenta agent"
requires-python = ">=3.11"
dependencies = [
    "runner-shared[mongodb,tracing]",
    "magenta-sdklanggraph",
    "langchain-core>=0.3",
    "langchain-openai>=0.2",
    "langchain-anthropic>=0.2",
    "langchain-google-genai>=2.0",
    "langgraph>=0.2",
    "python-dotenv>=1.0.0",
]

[project.scripts]
contract-comparator = "contract_comparator.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contract_comparator"]

[tool.uv.sources]
runner-shared = { git = "https://github.com/10gen/magenta-client-libraries", subdirectory = "packages/runner-shared" }
magenta-sdklanggraph = { git = "https://github.com/10gen/magenta-client-libraries", subdirectory = "packages/magenta-sdklanggraph" }

[tool.ruff]
line-length = 100
target-version = "py311"
