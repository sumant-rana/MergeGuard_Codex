[project]
name = "mergeguard-agent-truth-report-synthesizer"
version = "0.2.0"
description = "MergeGuard truth-report-synthesizer Magenta agent"
requires-python = ">=3.11"
dependencies = [
    "runner-shared[mongodb,tracing]",
    "magenta-sdklanggraph",
    "python-dotenv>=1.0.0",
]

[project.scripts]
truth-report-synthesizer = "truth_report_synthesizer.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/truth_report_synthesizer"]

[tool.uv.sources]
runner-shared = { git = "https://github.com/10gen/magenta-client-libraries", subdirectory = "packages/runner-shared" }
magenta-sdklanggraph = { git = "https://github.com/10gen/magenta-client-libraries", subdirectory = "packages/magenta-sdklanggraph" }

[tool.ruff]
line-length = 100
target-version = "py311"
